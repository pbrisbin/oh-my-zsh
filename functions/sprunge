# vim: set ft=zsh:
function sprunge() {
  local url

  if (( $# )); then
    read -r url < <(curl -s -F 'sprunge=<-' http://sprunge.us < "$1")

    echo "$url?${1##*.}"
  else
    # normal, stdin usage
    curl -F 'sprunge=<-' http://sprunge.us
  fi
}
